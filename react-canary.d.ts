/// <reference types="react/canary" />

/**
 * `<ViewTransition>` vive en los tipos canary de React.
 *
 * No es un experimento gratuito: es el movimiento ② del encargo — la fila de la
 * sala de control **se convierte** en la cabecera del episodio. El App Router
 * corre sobre releases canary de React, así que el componente existe en tiempo
 * de ejecución; lo único que falta es declarar sus tipos.
 */
